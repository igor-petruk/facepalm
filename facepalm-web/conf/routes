GET     /                                   		Application.index
GET		/count/{siteUrl}/{imageUrl}					Application.count
GET		/like/{siteUrl}/{imageUrl}/{logonUserId}	Application.like

GET     /favicon.ico                            404
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}
